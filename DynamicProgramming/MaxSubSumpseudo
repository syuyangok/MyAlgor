// Give a array, calculate the continuous subsequence with max sum

MaxSubSum(a)
  create a array L[n] to record max sum of ai
  L0 =a0;
  for i=1-->n-1:
      if L(i-1) >=0: Li= L(i-1)+ai;
      else Li= ai;
  
  //find max value in L[n]
  maxSum = L0
  for i=0--> n-1:
      if(Li>maxSum):  maxSum = Li
  
  //return result
  
  return result;
